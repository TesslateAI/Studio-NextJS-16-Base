{
  "apps": {
    "nextjs-16-base": {
      "directory": ".",
      "port": 3000,
      "start": "bun install --frozen-lockfile && bun run dev -- --hostname 0.0.0.0 --port 3000"
    }
  },
  "primaryApp": "nextjs-16-base"
}
